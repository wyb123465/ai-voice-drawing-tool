# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [1.0.0] - 2026-06-12

### Added
- **核心语音绘图功能**：支持圆形、矩形、三角形、线条、星星、太阳、山、树、波浪线等 9 种图形
- **中文语音识别**：基于 Web Speech API 的连续语音识别，支持实时转录
- **复杂场景拆解**：单句"画一个太阳，下面有两座山，山前面有一棵树"自动拆解为多个绘图指令
- **相对位置布局**：支持"在...右边/左边/上面/下面/前面"的空间关系描述
- **对象编辑能力**：变大/变小、移动、旋转、换色、删除、复制
- **撤销/重做/清空/导出**：完整的画布操作历史管理
- **文字标注**：语音添加文字到画布
- **颜色系统**：支持红、蓝、浅蓝、绿、黄、橙、棕、紫、粉、黑、白、灰等中文颜色别名
- **位置系统**：九宫格位置（中央、四边、四角）

### Added - AI 能力
- **LLM 兜底机制**：规则优先、低置信度才调用云端大模型，默认离线零成本
- **OpenAI 兼容接口**：可接入任意 OpenAI 兼容的大模型服务
- **白名单校验**：云端开放输出必须通过形状/颜色/位置白名单校验，确保安全
- **未知对象阻断**："把猫变大"等未知对象名会返回明确提示，不会误操作
- **演示模式**：`?llmdemo=1` 参数支持无密钥演示云端兜底链路

### Added - 工程能力
- **串行化指令队列**：修复连续语音识别时异步竞态导致的状态覆盖问题
- **目标选择**：支持 `first/last` 与序号选择（"删除第一个圆"、"删除第二个圆"、"删除刚才的圆"）
- **颜色过滤编辑**：支持"把红色圆形变大"这类带颜色限定的编辑指令
- **文本切分保护**："写上再见"会保留完整文字，同时"再画一个矩形"仍可作为新命令切分
- **双击保护**：捕获 `InvalidStateError` 避免快速双击启动按钮时崩溃
- **云端错误回退**：网络失败或云端异常时自动降级到本地规则
- **密钥安全**：API 密钥仅通过 `Authorization` 头传递，不出现在 URL 或日志

### Added - Demo 能力
- **文本回放模式**：`?replay=1` 支持无麦克风环境的文本输入复现
- **批量回放**：`?demo=指令1|指令2|...` 支持自动批量执行指令序列
- **来源标注**：日志显示每条指令的解析来源（规则/云端/云端·模拟）

### Added - 测试与文档
- **72 个自动化测试**：覆盖命令解析、状态管理、LLM 兜底、OpenAI resolver、澄清追问、连续聆听、评审脚本数据
- **完整设计文档**：架构说明、能力清单、未完成项说明、成本与隐私策略
- **README 文档**：快速运行、支持指令、Demo 流程、技术实现、提交清单
- **提交材料补强**：补充用户价值、提交说明、评审前清单、PR 模板和 MIT License
- **评审体验面板**：首屏提供一键评审演示、逐步脚本按钮和运行看板
- **AI 兜底可见化**：首屏显示 LLM 兜底状态，并提供无密钥模拟示例入口
- **一轮澄清追问**：低置信移动表达先反问，听到确认后再执行；支持“整个三角形上去”“把它上去”“把这个圆上去”这类焦点指代口语句，并会在点名形状不存在时阻断澄清、缩放和换色，避免确认或执行无法完成的动作
- **连续聆听恢复**：Web Speech API 在句子结束、静默、`no-speech`、`aborted` 或临时 `network` 错误后会自动重连，用户不需要每说一句都重新点击启动语音

### Technical
- 纯前端实现，无后端依赖
- 零第三方运行时依赖（测试使用 Node.js 内置 test runner）
- 不可变状态管理，支持完整的撤销/重做历史
- 模块化架构：命令解析、状态引擎、渲染器完全分离
- 约 2500 行代码，15 个模块

## [0.1.0] - 2026-06-12

### Initial Release
- 项目脚手架搭建
- 基础命令解析器原型
- Canvas 2D 渲染引擎
- 简单的语音识别集成
